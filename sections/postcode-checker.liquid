{{ section.settings.postcode_checker }}
{% schema %}
{
   "name": "Postcode Checker",
   "settings": [
   {
      "type": "liquid",
      "id": "postcode_checker",
      "label": "Postcode Checker",
      "info": "Enter postcode to check delivery day and cost."
   }],
   "presets": [
   {
      "name": "Postcode Checker"
   }]
}
{% endschema %}